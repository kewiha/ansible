---

#- name: get processor info
#  hosts: debian
#  become: true
#  tasks:
#    - name: cat proc info
#      debug:
#        msg: 
#          - "cpu vendor is {{ ansible_processor[1] }}"

- name: run debian common tasks
  hosts: debian
  become: true
  handlers:
    - import_tasks: handlers.yml
  roles: 
    - debian

- name: run gnome3 tasks
  hosts: gnome3
  become: true
  handlers:
    - import_tasks: handlers.yml
  roles:
    - gnome3

- name: run firefox tasks
  hosts: firefox
  become: true
  handlers:
    - import_tasks: handlers.yml
  roles:
    - firefox
